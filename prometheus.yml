global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: resilient-metrics
    static_configs:
      - targets: ["host.docker.internal:3116"]

rule_files:
  - "sla-rules.yml"
