apiVersion: apps/v1
kind: Deployment
metadata:
  name: onnx-sidecar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: onnx-sidecar
  template:
    metadata:
      labels:
        app: onnx-sidecar
    spec:
      containers:
        - name: onnx
          image: onnx-runtime:latest
