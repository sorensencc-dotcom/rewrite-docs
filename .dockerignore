# Build artifacts & caches (can be regenerated in container)
node_modules
.git
dist
build
coverage
.next
.pytest_cache
__pycache__
*.pyc
rewrite-mcp/projects/*/node_modules

# Temporary & transient files
.venv
*.venv
venv
logs
data/temp
charlie-deep-research

# IDE & workspace metadata
.ijfw
.claude

# Secrets (explicit — use .env.example instead)
.env*
!.env.example

# OS
.DS_Store

# Allow everything else for Docker builds
# Dockerfiles will COPY only what they need
